# 驗證報告與實機驗收

日期：2026-09-23。驗證分層，不把示範或 mock 測試當成教學品質證據。

## 已驗證

| 層級 | 結果與範圍 |
|---|---|
| 本機後端 | Python／FastAPI 能啟動，localhost 頁面可使用 |
| Python 自動測試 | 43 項：四則核算、六科示範、確認／解答門檻、雲端開關、CSRF／來源、圖片格式、快取與清除、Codex 協定、OAuth URL、拒絕工具與 API Key auth；另 10 項檢查 App 程序識別、外部服務拒絕接手、既有服務接手、新程序啟動、正常關閉、PID 重用與壞檔防護 |
| 本機動作邏輯 | 3 項：穩定等待、不重送、分析間隔、停筆冷卻、延後、雜訊 |
| Chromium 流程 | 14 項：提示→解釋→複習、下一題才能切科、fake webcam／暫停、手機寬度、OAuth 視窗、清除後舊回應不回填、四種翻轉組合的預覽／非置中 ROI 截圖像素一致、畫面大幅變化／停筆／翻轉／框選／重開鏡頭不清教學與作答、辨識途中移動及下一題擋住延遲教學、取消首次讀題可重試、全頁遮罩涵蓋兩段推理／背景操作阻擋、讀題失敗解除遮罩、教學失敗解除遮罩、取消／Escape／舊回應不關閉新遮罩 |
| 視覺檢查 | 桌面 1440 px 與手機 390 px 截圖，文字、控制與版面可讀；全頁處理遮罩與取消按鈕於兩種寬度皆可見 |
| 真實 Codex 控制面 | 本機 Codex 0.155.0-alpha.16.3 啟動、initialize、account/read（未登入）、account/login/start 返回官方 HTTPS URL、取消及登出成功 |
| Mac App 實機 | macOS 27：建立／本機簽署／Launch Services 登錄成功；8877 測試 App 啟動、接手、按鈕退出、重新啟動與 Command-Q 停止成功；正式 App 接手原 8765 服務、退出及重新啟動成功，圖示與控制對話框可見。這不是其他 Mac 的安裝驗收 |
| 靜態檢查 | Ruff、JavaScript 語法、Git diff whitespace 檢查 |

OAuth 瀏覽器測試只模擬官方登入頁，不代替使用者登入。真實控制面驗證也只到網址與 callback 服務啟動，沒有取得使用者登入授權。

測試可重跑：

```bash
uv run ruff check .
uv run pytest -q
npm test
npx playwright test
```

GitHub workflow 會跑上述測試；遠端執行狀態請以 Repo Actions 的實際結果為準。測試沒有送真實學生資料，沒有消耗模型推理額度。

## 待完成：第一台 Mac 驗收

| 項目 | 操作 | 通過條件 | 目前 |
|---|---|---|---|
| 真人 OAuth | 按登入、官方網頁登入、回來檢查 | 顯示 ChatGPT auth，重開仍可登入，登出僅影響本專案 | 待測 |
| 模型權限 | 讀一張自己寫的合成算式圖片 | 實際所選模型回覆符合 Observation schema | 待測 |
| 多科教學 | 數學、英文、國語各 5 題 | 文字、圖像、提示階梯、說明、複習能完成 | 待測 |
| USB 鏡頭 | 選 webcam、框題、換位置 | 裁切與實際題目一致，沒有把框外資料送出 | 待測 |
| 手寫辨識 | 真筆跡、修改、擦除、手遮住 | 看不清會降級，不對錯字做武斷批改 | 待測 |
| 停筆 | 寫 20 秒、停 30 秒、繼續寫 | 在門檻後提醒、不在書寫中頻繁打斷 | 待測 |
| 語音 | 安裝 zh-TW 聲音、朗讀／暫停 | 可理解、無重疊，取消立即停止 | 待測 |
| 斷網／限流 | 推理期間中斷網路或模擬配額 | 停自動分析、保留可操作畫面、不偷偷換帳號或 API | 待測 |
| 教學保留 | 揮手、翻頁、繼續書寫與停筆 | 教學與複習作答保留，主動按下一題才清空 | 合成鏡頭已測，實機待測 |
| 過期回應 | 分析中換題、暫停、清除 | 舊題不回填，快取不重新出現 | mock 已測，真實待測 |
| 長時間 | 連續 60 分鐘 | 記憶體、熱、用量與延遲可接受 | 待測 |

本機沒有完成這些人工／實體測試，因此不宣稱「已可可靠陪讀」、「所有科目準確」或「已符合兒童產品上市條件」。

## 教學評測紀錄欄位

每題記錄：匿名 case ID、科目、年級、圖片條件、教師題目／作答真值、模型擷取、模型判讀、核算結果、兩層提示是否洩漏、解釋正確性、複習題可解性、學生無提示變式題結果、模型 ID、Codex 版本、延遲、tokens。

測試圖片若需保存，必須另外取得同意並存至排除版控的位置；公開 Repo 只放合成或明確授權素材。

## 獨立安裝包驗證（2026-09-24）

- v0.3.0 Apple Silicon，內附 Python 3.12.13 與官方公開 Codex 0.156.1；宣告 macOS 13 起，實際驗證機為 macOS 27。
- Python 46 項、動作邏輯 3 項、Chromium 14 項通過；新增外部資料目錄、含空格的搬移路徑與程序身分核對測試。
- 將 App 複製到新的臨時資料夾，限制 PATH 為系統目錄，使用全新資料目錄：內附 Python／依賴／Codex 載入、啟動、重複啟動接手、停止與重啟通過。
- 內附 Codex 的 initialize、未登入查詢、官方 OAuth 網址產生、取消／登出及示範教材通過；沒有真人登入或模型推理。
- 搬移及啟停後，App 的 codesign 深度／嚴格完整性驗證通過；未發現指向 App 外的符號連結或內附 auth.json／.runtime。
- 使用同一啟動器、改用 8878 埠的 QA App，在原生控制對話框按「關閉服務並退出」後，App 與服務程序均結束；沒有中斷原本 8765 的服務。
- 這些結果不代表其他 Mac、Gatekeeper、macOS 13 實機、真人登入、手寫與真實模型已完成驗收。發行版尚缺 Developer ID 與 Apple 公證。

重跑獨立包檢查：

```bash
uv run python scripts/check_standalone.py /path/to/AIStudyPartner.app
```

此檢查只操作全新測試資料目錄與臨時複本，使用動態空閒埠，不使用作者帳號或真實作業。

## 模型選單（v0.3.1）

提供 GPT-6 Astra、Sol、Luna，保留官方型號名稱；品牌、等待提示及錯誤文字改成通用 AI。每次辨識／教學都指定選單模型，快取依模型隔離；切換保留教學與作答，處理中鎖定選單，瀏覽器記住偏好。

使用 mock 驗證三種模型均傳到 Codex 介面、不跨模型使用快取、拒絕未知型號與失敗不切換模型。瀏覽器驗證偏好保存、教學保留、辨識與教學模型一致及處理中鎖定。沒有執行各模型的真實推理或帳號權限測試。
